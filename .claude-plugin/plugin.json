{
  "name": "code-search",
  "description": "Search across many repositories at once — who calls an endpoint, where a symbol is defined, what implements an interface, which repo publishes a package, and when a cross-repo seam last changed. Routes each question to whichever of five engines can answer it, and labels every answer with the kind of evidence behind it.",
  "version": "1.6.1",
  "author": {
    "name": "Zvi Rahamim"
  },
  "homepage": "https://github.com/my-organization-for-testing-067/code-search-fleet",
  "keywords": [
    "search",
    "monorepo",
    "multi-repo",
    "code-navigation",
    "ripgrep",
    "ast-grep",
    "ctags",
    "lsp"
  ]
}
