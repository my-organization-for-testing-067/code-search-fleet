// The module name a consumer substitutes when it includes this build. It has to
// match the artifactId in the publishing block, or the composite substitution
// of com.acme:pricing-lib silently does not apply.
rootProject.name = "pricing-lib"
